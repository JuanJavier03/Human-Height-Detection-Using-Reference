### Tareas realizadas:
- Creción de la carpeta `bitacora` para almacenar los registros diarios de actividades.
- Creación del archivo `18_11_2025.md` dentro de la carpeta `bitacora`.
- Actualización del archivo `README.md` 
- Creación del archivo `pasos.md` con los pasos a implementar en el proyecto.
- [x] Implementado el `yolov8n.pt` de YOLOv8 entrenado para detectar tanto personas como objetos
  - [x] Modificado el script `object-detection.py` para cargar el modelo `yolov8n.pt` y realizar detecciones en tiempo real.
  - [x] Creación de una nueva carpeta llamada `.\capturas` para almacenar las imágenes capturadas durante la detección.

### Atención:
Para ejecutar el script `object-detection.py` emplear el siguiente comando en la terminal:
 ```bash
python "object-detection.py" 2>&1 | grep -v "NNPACK"
```