### Tareas realizadas:

- [x] Corrección del archivo `human-detector.py` para solucionar el error de visualización de la imagen con las cajas delimitadoras, puesto que al ejecutar el archivo, no se mostraba la ventana con la imagen procesada (detectando a las personas así como mostrando las cajas delimitadoras):
  ```python
    # Verificar que la imagen se cargó correctamente
    if imagen_con_cajas is None or imagen_con_cajas.size == 0:
        print("Error: la imagen no se pudo procesar correctamente")
        return cajas
    
    print(f"Mostrando imagen de tamaño: {imagen_con_cajas.shape}")
    cv2.namedWindow("Detección de personas (YOLOv8)", cv2.WINDOW_NORMAL)
    ```
    Con esta corrección, se asegura que la imagen se ha procesado correctamente antes de intentar mostrarla, evitando así errores en tiempo de ejecución. Además de redimensionar la propia ventana a un tamaño normal, evitando desbordamientos por si la imagen es demasiado grande para la pantalla. En algunos sistemas Linux, provoca que la ventana salga complementamente en negro.

    Para usar el archivo sin el warning de NNPACK! emplear el siguiente comando:
    `python human-detector.py ruta/a/mi_imagen.jpg 2>&1 | grep -v "NNPACK"`

- [x] Corrección del archivo `folio-detector-pasos.py` para mejorar la visualización de las imágenes en cada paso del proceso de detección del folio. Para ello, se han eliminado caracteres especiales propios del español, como las vocales acentuadas y caracteres matemáticos (≅ para el paso 6).

- [x] Breve corrección en la descripción del archivo `detector-altura.py` para reflejar correctamente su nombre.

- [x] Corrección del archivo `human-detector-webcam.py` para evitar la creación masiva de ventanas negras. Se eliminó la inferencia en modo `stream=True` y ahora sólo se procesa el primer resultado devuelto por YOLO en cada frame (`resultados[0]`), garantizando que `cv2.imshow` y `cv2.waitKey(1)` se ejecuten por frame y mantengan una única ventana estable. Ya que anteriormente, al usar `stream=True`, se creaban múltiples ventanas que se solapaban y causaban problemas de visualización. Quedando así:

  ```python
    # Predicción frame a frame, con los mismos parámetros de precisión del script original
    resultados = modelo(frame, conf=0.55, iou=0.7, verbose=False)
    resultado = resultados[0]

    frame_anotado = resultado.plot()
    cajas_xyxy = resultado.boxes.xyxy.tolist() if resultado.boxes is not None else []
    cajas = [(x1, y1, x2, y2) for (x1, y1, x2, y2) in cajas_xyxy]

  ```
  Para usar el archivo sin el warning de NNPACK! emplear el siguiente comando:
    `python human-detector-webcam.py  2>&1 | grep -v "NNPACK"`

- [x] Ajuste del archivo `detector-altura-webcam.py` para unificar la ventana de visualización. Se creó una única ventana previa con nombre ASCII (`"Deteccion Humano + Folio + Altura"`) usando `cv2.namedWindow(..., cv2.WINDOW_NORMAL)` y se reutilizó el mismo identificador en `cv2.imshow`. Así se evita que HighGUI cree múltiples ventanas negras al actualizar cada frame, manteniendo una sola vista estable de la detección integrada. 
  
### Implementaciones realizadas:

- [x] Implementación de un filtro de mediana para suavizar las estimaciones de altura en el archivo `detector-altura-webcam.py`. Se ha utilizado la estructura `deque` para almacenar las últimas 5 estimaciones de altura y se ha calculado la mediana de estos valores para obtener una estimación más estable y menos propensa a fluctuaciones bruscas. 
- [x] Se ha cambiado el color de "Altura" y "Altura media" a un color más visible desde tan lejos del objetivo. Además, se han añadido líneas de debug. 